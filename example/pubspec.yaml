name: example
description: Example for the Appinio Swiper

publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_card_swiper:
    path: ../

flutter:
  uses-material-design: true
